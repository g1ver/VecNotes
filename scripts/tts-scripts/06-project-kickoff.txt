Had the project kickoff call today for the new client. They want a data pipeline that ingests real-time sensor readings from factory floor equipment, detects anomalies, and triggers alerts. The main technical challenge is the volume — about 50,000 events per second at peak. We're going to evaluate Kafka versus a simpler Redis Streams approach first before committing. First sprint is focused on getting a prototype ingestion layer running.
